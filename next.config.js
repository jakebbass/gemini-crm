
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // You might need to add hostname for images if using next/image with external sources
  // images: {
  //   remotePatterns: [
  //     {
  //       protocol: 'https',
  //       hostname: 'picsum.photos',
  //     },
  //   ],
  // },
};

module.exports = nextConfig;
